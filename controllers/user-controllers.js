const userModel = require("../models/user-model");
/**
 *
 * User google auth controller
 * @param {*} req
 * @param {*} res
 * @param {*} next
 *
 */
const auth = (req, res, next) => {};

exports.auth = auth;
